# Phase 3 (optional): steps × cfg_scale fine-tuning on phase-1/2 winners.
# Requires winners.yaml phase1_noise and phase2_prompts completed.
phase: phase3_diffusion

variants:
  - id: steps6_cfg0.8
    steps: 6
    cfg_scale: 0.8
    note: "Faster, looser guidance"
  - id: steps8_cfg1.0
    steps: 8
    cfg_scale: 1.0
    note: "Production default budget"
  - id: steps10_cfg1.2
    steps: 10
    cfg_scale: 1.2
    note: "Slower, slightly stronger guidance"
