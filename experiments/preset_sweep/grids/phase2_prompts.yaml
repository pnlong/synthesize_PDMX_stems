# Phase 2: prompt_variant sweep on phase-1 per-category noise winners.
# Requires winners.yaml phase1_noise completed.
phase: phase2_prompts
steps: 8
cfg_scale: 1.0

variants:
  - id: current
    prompt_variant: current
    note: "Full caption pipeline as implemented"
  - id: minimal
    prompt_variant: minimal
    note: "Shorter / sparser prompt"
  - id: preservation
    prompt_variant: preservation
    note: "Emphasize keeping melody, rhythm, timing"
